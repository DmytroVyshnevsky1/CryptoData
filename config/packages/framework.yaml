# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Enable Redis cache adapter
    cache:
        app: 'cache.adapter.redis'
        default_redis_provider: 'redis://redis:6379'

    session: true